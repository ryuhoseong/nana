### 목록
    export

### 등록
    vi ~/.bash_profile
    ex) export JAVA=[경로]

    source ~/.bash_profile

### alias 설정
    vi ~/.bashrc
```
alias ll='ls -al'
alias ls='ls --color=auto'
```
    source ~/.bashrc